import { Skeleton } from "@mui/material";

const NavbarSkeleton = () => {
    return (
        <>
            <Skeleton variant="rect" width={250} height={60} />
        </>
    );
}

export default NavbarSkeleton;